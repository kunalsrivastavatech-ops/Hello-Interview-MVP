/* Styling setup matching your original black-white-green brand identity scale */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    background-color: #ffffff; /* Keeping consistent with your landing page's clean background layout */
    color: #000000;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}

/* Navbar panel configuration matching welcome.html properties */
header {
    background-color: #051405; /* Your trademark forest dark green navigation bar fill */
    border-bottom: 2px solid #10b981; /* Neon green indicator bar alignment */
    padding: 18px 40px;
    width: 100%;
}

.nav-container {
    max-width: 1300px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 22px;
    font-weight: 800;
    letter-spacing: 1px;
    color: #ffffff;
}

.logo span { color: #10b981; }

.portal-tag {
    color: #ffffff;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.5px;
    background-color: #111827;
    padding: 6px 14px;
    border: 2px solid #ffffff;
    border-radius: 8px;
}

/* Center Form Wrapper Space */
.setup-wrapper {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 40px 20px;
}

/* Figma bounding structural card configuration template */
.setup-card {
    background-color: #ffffff;
    border: 3px solid #000000;
    border-radius: 24px;
    width: 100%;
    max-width: 600px;
    padding: 40px;
    box-shadow: 0 4px 0px #000000; /* Neo-brutalism flat offset shadow line matching your image elements */
}

.card-header {
    margin-bottom: 30px;
}

.card-header h2 {
    font-size: 24px;
    font-weight: 900;
    letter-spacing: -0.5px;
    margin-bottom: 8px;
}

.card-header p {
    font-size: 14px;
    color: #4b5563;
    font-weight: 500;
}

/* Form Input Layout Blocks */
.form-group {
    margin-bottom: 22px;
}

label {
    display: block;
    font-size: 11px;
    font-weight: 800;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    margin-bottom: 8px;
}

input[type="text"], select {
    width: 100%;
    padding: 14px 16px;
    border: 3px solid #000000;
    border-radius: 12px;
    font-size: 15px;
    font-weight: 600;
    outline: none;
    background-color: #ffffff;
}

input[type="text"]:focus, select:focus {
    border-color: #3b82f6; /* Switches color to your primary actions blue frame element when clicked */
}

/* System warning indicator container box layout geometry */
.warning-box {
    background-color: #fff176; /* Yellow warning block matching your Figma hello card fill properties */
    border: 3px solid #000000;
    border-radius: 16px;
    padding: 16px 20px;
    margin-bottom: 25px;
    display: flex;
    gap: 15px;
    align-items: flex-start;
    box-shadow: 2px 2px 0px #000000;
}

.warning-star {
    background-color: #ff5722; /* Coral red star container node fill matching image elements */
    color: white;
    width: 24px;
    height: 24px;
    font-size: 11px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    border: 2px solid #000000;
    flex-shrink: 0;
}

.warning-box p {
    font-size: 13px;
    line-height: 1.5;
    font-weight: 600;
}

/* Checkbox alignment structures options items wrapper styles */
.checkbox-group {
    display: flex;
    flex-direction: column;
    gap: 14px;
    margin-bottom: 35px;
}

.check-item {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    cursor: pointer;
    font-size: 13px;
    font-weight: 600;
    color: #1f2937;
}

.check-item input[type="checkbox"] {
    width: 18px;
    height: 18px;
    accent-color: #10b981; /* Green ticks matching brand core theme actions identity colors */
    margin-top: 1px;
    cursor: pointer;
}

/* Main Submission Call-To-Action Layout Launcher Engine button element properties */
.btn-launch {
    width: 100%;
    background: linear-gradient(135deg, #10b981, #059669); /* Bright green gradient fill alignment */
    color: #ffffff;
    border: 3px solid #000000;
    padding: 16px;
    border-radius: 14px;
    font-size: 16px;
    font-weight: 800;
    letter-spacing: 0.5px;
    cursor: pointer;
    box-shadow: 0 4px 0px #000000;
    transition: transform 0.1s, box-shadow 0.1s;
}

.btn-launch:active {
    transform: translateY(2px);
    box-shadow: 0 2px 0px #000000;
}
